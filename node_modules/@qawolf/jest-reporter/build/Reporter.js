"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const kleur_1 = require("kleur");
class Reporter {
    constructor(globalConfig, options) {
        this._globalConfig = globalConfig;
        this._options = options;
    }
    onRunComplete(_, aggregatedResult) {
        console.log('');
        aggregatedResult.testResults.forEach(testResult => {
            if (testResult.failureMessage) {
                console.log(testResult.failureMessage);
            }
            testResult.testResults.forEach(assertionResult => {
                if (assertionResult.status === 'passed') {
                    console.log(kleur_1.green('pass:'), assertionResult.fullName);
                }
                else if (assertionResult.status === 'failed') {
                    console.log(kleur_1.red('fail:'), assertionResult.fullName);
                }
                assertionResult.failureMessages.forEach(message => console.log(message));
            });
        });
    }
}
exports.Reporter = Reporter;
//# sourceMappingURL=Reporter.js.map